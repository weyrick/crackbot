import lib.irc Server;
import crack.io cout;
import crack.lang die;

i := Server("irc.roadsend.com", 6667, 'crack|','crackbot','http://crack-lang.org');
//i := Server("irc.freenode.net", 6667, 'crack|','crackbot','http://crack-lang.org');
if (i.connect()) {
    cout `connected!\n`;
}
else
    die("unable to connect");
i.join("#crack-lang");

while (1) {
    i.process();
}

//i.say("kachow");
